#include "textflag.h"

TEXT ·synthesisEncryptCounterHardware(SB), NOSPLIT, $0-16
	MOVQ block+0(FP), AX
	MOVQ keys+8(FP), BX
	MOVOU (AX), X0
	PXOR 0(BX), X0
	AESENC 16(BX), X0
	AESENC 32(BX), X0
	AESENC 48(BX), X0
	AESENC 64(BX), X0
	AESENC 80(BX), X0
	AESENC 96(BX), X0
	AESENC 112(BX), X0
	AESENC 128(BX), X0
	AESENC 144(BX), X0
	AESENCLAST 160(BX), X0
	MOVOU X0, (AX)
	RET
